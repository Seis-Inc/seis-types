{
  "name": "seis-types",
  "version": "1.0.18",
  "main": "dist/index.js",
  "author": "Seis Inc",
  "license": "ISC",
  "scripts": {
    "build": "./node_modules/typescript/bin/tsc && cp -R src/ledger-service dist/ && cp -R src/core-api dist/",
    "createVersion": "git tag 1.0.18",
    "publishVersion": "git push --tags"
  },
  "devDependencies": {
    "typescript": "^5.4.2"
  }
}
